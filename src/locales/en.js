// English — scaffolded identical to PT for now.
// Replace values with real English translations when localization ships.
import pt from './pt'

const en = JSON.parse(JSON.stringify(pt))
export default en
